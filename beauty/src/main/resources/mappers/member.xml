<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 강중현 -->
<mapper namespace="kr.co.beauty.dao.MemberDAO">
	<select id="selectTerms" resultType="MemberVO">
		select * from `terms`;
	</select>

	<insert id="insertMember">
		insert into `member` set
			`uid`=#{uid},
			`pass`=#{pass},
			`name`=#{name},
			`zip`=#{zip},
			`addr1`=#{addr1},
			`addr2`=#{addr2},
			`phone`=#{phone},
			`regip`=#{regip},
			`rdate`=NOW(),
			`height`=#{height},
			`weight`=#{weight};
	</insert>
	
	<!-- 아이디 찾기 -->
	<select id="findId" resultType="String">
		select `uid` from `member` where `name`=#{arg0} and `phone`=#{arg1};
	</select>
	
	<select id="findPw" resultType="String">
		select `pass` from `member` where `name`=#{arg0} and `uid`=#{arg1} and `phone`=#{arg2};
	</select>

	<update id="findPwChange">
		update `member` set `pass`=#{pass} where `uid`=#{uid};
	</update>
	
	
	<!--  -->
	<select id="selectMember" resultType="Member1VO">
		SELECT * FROM `member`
		WHERE `uid` = #{uid}
	</select>
	
</mapper>